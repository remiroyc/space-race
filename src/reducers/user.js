const initialState = {
  distance: 0,
  shipChoice: 1
}

function userReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default userReducer
